# Legacy Code

This folder keeps the original single-file prototype used before the project
was refactored into the current modular structure.

- `fx_trading.py`: original monolithic IBKR FX paper trading script

It is preserved for reference only and is not the recommended entrypoint for
running the project. Use `main.py` at the project root instead.
